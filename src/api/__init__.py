"""Interface layer — FastAPI backend.

Serves projections and decisions to the Next.js frontend. Kept thin until the
Phase 1 analysis is trustworthy (design.md §3.4). The app object lives in
``src.api.main:app``.
"""
